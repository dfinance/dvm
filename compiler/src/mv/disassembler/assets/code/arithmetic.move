module M {
   resource struct R {
           f: u64
   }

   fun t0() {
       0 + 0;
       1 + 0;
       0 + 1;
       0 + (1: u8);
       (0: u8) + 1;
       0 + (1: u128);
       (0: u128) + 1;
       (0) + (1);
   }

   fun t1() {
      1 + 1 * 2;
      1 + (1 * 2);
      (1 + 1) * 2;
      2 * 2 / 2;
      1 % 2;
      1 + 1 % 2;
      1 / 1 % 2;
      2 * 1 % 2;
      2 * 1 ^ 2;
      1 & 1;
      1 | 1;
   }

   fun t2() {
        !true;
        true && false;
        true || false;
        1 > 2;
        1 >= 2;
        1 < 2;
        1 <= 2;
   }

   fun t3() {
       0 << 1;
       0 >> 1;
   }
}